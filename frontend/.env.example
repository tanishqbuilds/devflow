# Public URLs used by the browser. Do not put secrets in NEXT_PUBLIC_* values.
NEXT_PUBLIC_API_URL=http://localhost:8010
NEXT_PUBLIC_WS_URL=ws://localhost:8010

# Optional: leave empty to use the built-in mock authentication flow.
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=
